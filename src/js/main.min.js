const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    dots : true,
    autoplay: true,
    infinite: true,
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    autoplay: true,
    infinite: true,
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    autoplay: true
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    autoplay: true,
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    autoplay: true,
    rtl: true
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    autoplay: true,
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    infinite: true,
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick({
    infinite: true,
    slidesToShow: 3,
    slidesToScroll: 3
});
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

$('.slider').slick();
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}


const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}

console.log($);
const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(topMenu);
    topMenu.classList.toggle('show');
}


const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(3);
    topMenu.classList.toggle('show');
}


const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    console.log(3);
    topMenu.classList.toggle('shwo');
}


const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');
    
    topMenu.classList.toggle('shwo');
}


const navMenu = document.getElementById('menu');

navMenu.onclick = (e) => {
    const topMenu = document.getElementById('top-menu');

    topMenu.classList.toggle('shwo');
}


const navMenu = document.getElementById('#menu');

navMenu.onclick = (e) => {
    const 
}


const navMenu = document.getElementById('#menu');




const navMenu = document.getElementById('#menu');


const navMenu = document.querySelector('.header__menu');


const navMenu = document.querySelector('.header__menu');

nav
const navMenu = document.querySelector('.header__menu')
const navMenu = document.querySelector('.')